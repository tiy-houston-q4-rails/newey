<%= simple_form_for (@user ||= User.new), remote: true do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.submit class: "btn btn-block btn-lg btn-success" %>
<% end %>
